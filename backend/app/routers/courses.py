from fastapi import APIRouter

router = APIRouter()

# TODO: POST   /              — create course (professor)
# TODO: GET    /              — list my courses
# TODO: GET    /{id}          — get course detail
# TODO: POST   /{id}/join     — join course via invite code (student/TA)
# TODO: GET    /{id}/members  — list course members
