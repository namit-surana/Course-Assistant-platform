from fastapi import APIRouter

router = APIRouter()

# TODO: GET    /presigned-url         — get S3 presigned URL for PPT or video upload
# TODO: POST   /                      — submit (after files uploaded to S3)
# TODO: GET    /{id}                  — get submission + status
# TODO: GET    /team/{team_id}        — get team's submission for an assignment
