from fastapi import APIRouter

router = APIRouter()

# TODO: POST   /courses/{course_id}/teams   — create team (student)
# TODO: POST   /{id}/join                   — join team by ID (student)
# TODO: GET    /{id}                        — get team + members
# TODO: DELETE /{id}/members/{user_id}      — leave / remove from team
