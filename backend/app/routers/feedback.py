from fastapi import APIRouter

router = APIRouter()

# TODO: GET    /{submission_id}        — get full feedback (scores + comments)
# TODO: PATCH  /scores/{score_id}      — override a score (TA / professor)
