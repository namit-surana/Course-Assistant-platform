from fastapi import APIRouter

router = APIRouter()

# TODO: POST /register         — professor email/password registration
# TODO: POST /login            — professor email/password login → JWT
# TODO: GET  /google/callback  — Cognito Google OAuth callback → JWT
# TODO: GET  /me               — return current user from JWT
